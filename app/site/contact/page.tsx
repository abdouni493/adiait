import { SiteContact } from "@/components/site/SiteContact";

/** Les coordonnées du club, réglées depuis « Site web › Coordonnées ». */
export default function SiteContactPage() {
  return <SiteContact />;
}
